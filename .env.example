# Same Mongo the rest of the data layer uses. The index is DB-backed on
# purpose: it accumulates history (firstSeen / lastPaid / miss streaks) that
# a regenerated file would lose.
DATABASE_URI=mongodb+srv://user:pass@host/dbname
