# Check out LF everywhere. Windows would otherwise convert to CRLF and the
# formatter fails the build on every file — a platform difference that has
# nothing to do with the code.
* text=auto eol=lf

# Never touch these.
*.png binary
*.jpg binary
*.webm binary
*.tgz binary
